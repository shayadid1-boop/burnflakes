node_modules/
.next/
.env
.env.local
.env*.local
.DS_Store
*.log
.vercel

# Cowork session outputs (not part of the app)
Claude outputs/
tsconfig.tsbuildinfo
src/app/favicon.ico

# not in the repo: too large to push through the browser bridge; commit it from the Mac when convenient
package-lock.json
