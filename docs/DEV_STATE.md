# מצב הפיתוח — 11.9.2026 (ערב)

## מה חי בענן
- `main` = כל שבועות 1–3 + תיקוני QA (קומיט אחרון: perf export + 0013). Vercel פורס אוטומטית; GitHub Actions מריץ מיגרציות 0000–0013 (כולן רצו בהצלחה).
- מיגרציות אחרונות: 0009 תאריכי 2026 · 0010 מניעת תרחיש כפול · 0011 תצוגות security_invoker + אין גישה ל‑anon · 0012 הוצאה ממתינה לא נספרת בספר החשבונות · 0013 תיקון טקסט משמרות הקמה.
- Supabase Auth: SMTP דרך ג'ימייל של שי (סיסמת אפליקציה, הוזן ידנית). Site URL + Redirect מוגדרים.

## איך Claude דוחף (הריפו לא נגיש מהענן)
`/home/claude/gen_diff_push.py` → מייצר `/home/claude/push_diff.js` (שינויים כ‑replace על העותק בגיטהאב, קבצים חדשים במלואם) → מריצים ב‑javascript_tool בטאב github.com בדפדפן המובנה → `git fetch && git reset --hard origin/main` בעותק המקומי. השיטה הישנה (`gen_push.py`, קבצים שלמים) עדיין קיימת.
כניסה לאתר לבדיקות: שי מעתיק את קישור הכניסה מהמייל ומדביק בשיחה; Claude פותח אותו בטאב `seed`. **יציאה** של שי כבר לא מנתקת את Claude (scope local).

## בדיקות בנייה שעברו (11.9, באתר החי, כמנהל)
כל המסכים נטענים; לא‑מחובר מופנה לכניסה; anon חסום לגמרי ב‑REST; תרחיש: יצירה, שינוי פרמטרים (37 + גיפט מושקע → 45,992 ₪), שכפול/אישור קיימים; מחלקה: רישום הוצאה; כספים: אישור הוצאה, ספר חשבונות; משמרות: תפקיד, פתיחה לפי ימים, הרשמה עצמית; חברים: יצירה, עריכה, "מגיע", לא פעיל; ייצוא אקסל (~5 שניות, 400KB).

## החלטות שנלקחו לבד (לאישור שי)
- הוצאות גלויות לכל החברים המחוברים; ספר החשבונות האישי — רק לבעליו ולמנהל.
- אישור תרחיש יוצר קופת "דמי קמפ" שווה לכולם + שורת הכנסה מתוכננת.
- תרחיש מאושר קפוא; לשינוי משכפלים. מחיקה רק לטיוטות.
- ראש מחלקה רושם הוצאה גם בשם אחר ומאשר ישירות; חבר רגיל רושם רק בשם עצמו וממתין.
- ייצוא אקסל: גיליון לכל טבלה + views + ledger, למנהל בלבד.

## פתוח
- w4t0 סבב לוגיקה של שי על התרחישים; w4t1 כניסה עם סיסמה (שי לא אוהב קישורי מייל); אופציה: כניסה עם Google.
- נשארו 2 טיוטות "2026 30 חברים" (36 ו‑37/מושקע) — שי ימחק את המיותרת.
- package-lock.json לא בריפו (גדול לדחיפה דרך הדפדפן); לדחוף מהמק בהזדמנות.
- סבב עיצוב "כמו הדמו" (שבוע 4).
