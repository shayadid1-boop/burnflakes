# מצב הפיתוח — 11.9.2026 (נכתב בזמן ששי בנסיעה)

## מה נבנה (בענן של Claude, מחכה לדחיפה)
קומיטים מקומיים מעל `8b62ca2` (w1t1): w1t2 התחברות · w1t3 RLS · w1t4/5 בונה תרחישים · w2t1–5 מסך מחלקה, רישום הוצאה, /me, /camp, כספים, לוח ניהול · w3t1–4 חברים, משמרות, ייצוא · QA.
מיגרציות חדשות: 0005_rls, 0006_scenario_tools, 0007_ledger_and_dues, 0008_qa_fk_fixes — נבדקו מול Postgres מקומי עם seed מלא.

## איך דוחפים כשהמק חוזר
1. מהמק (`$HOME/bfwork`): `git fetch && git reset --hard origin/main` ואז להעתיק את הקבצים מ‑camp planner (rsync) ולדחוף — **או** אם github חסום במק: להריץ `/home/claude/push_all.js` בדפדפן המובנה בטאב github.com (Git Data API, קומיט אחד).
2. אחרי הדחיפה: GitHub Actions מריץ 0005–0008 בענן; Vercel פורס.
3. סופאבייס → Authentication → URL Configuration: Site URL = https://burnflakes-woad.vercel.app (הוזן, לא בטוח שנשמר) + Redirect URL `https://burnflakes-woad.vercel.app/**`.
4. בדיקה ידנית עם שי צעד‑צעד (התחברות במייל, תרחיש 2025 = 44,390, אישור תרחיש 2026, הוצאה מהנייד, משמרת).

## החלטות שנלקחו לבד (לאישור שי)
- הוצאות גלויות לכל החברים המחוברים (שקיפות); ספר החשבונות האישי — רק לבעליו ולמנהל.
- אישור תרחיש יוצר אוטומטית קופת "דמי קמפ" עם תעריף שווה לכל המעמדים + שורת הכנסה מתוכננת "דמי קמפ" למאזן.
- תרחיש מאושר קפוא; לשינוי משכפלים. מחיקה רק לטיוטות.
- מסך מחלקה: ראש מחלקה רושם הוצאה גם בשם חבר אחר ומאשר ישירות; חבר רגיל רושם רק בשם עצמו, ממתין לאישור.
- ראש מחלקה מנהל תפקידי משמרת, פותח משמרות לפי תאריכים (רשימה), משבץ ומסיר; חברים נרשמים/מבטלים בעצמם.
- ייצוא אקסל: גיליון לכל טבלה + views + ledger, למנהל בלבד, בלוח הניהול.

## פתוח
- תאריכי מידברן 2026 (שי ישלים) → מיגרציה לעדכון events + milestones.
- SMTP חיצוני (Resend) — כרגע המייל המובנה של סופאבייס (מוגבל ~2–4 מיילים לשעה). מספיק לבדיקות; לפני הזמנת 37 חברים חובה להחליף.
- package-lock.json לא בריפו אם הדחיפה תיעשה דרך הדפדפן (גדול); לדחוף מהמק בהזדמנות.
